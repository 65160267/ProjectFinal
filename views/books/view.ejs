<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= book.title %></title>
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/performance.css" />
  <script src="/socket.io/socket.io.js"></script>
  </head>
  <body>
    <header class="site-header">
      <div class="header-inner container">
        <div class="brand">
          <a href="/dashboard" class="brand">
            SwapABook
          </a>
        </div>
        <!-- search removed per request -->
        <nav class="header-actions">
          <a href="/dashboard" title="Home">🏠</a>
          <a href="/books" title="Cart">🛍️</a>
          <a href="/messages" title="Messages">💬</a>
          <% if (typeof user !== 'undefined' && user && user.username) { %>
            <a href="/user" class="header-user" title="Your profile">
              <img class="user-avatar" 
                   src="<%= (user.avatar && user.avatar.startsWith('/')) ? user.avatar : (user.avatar ? '/uploads/' + user.avatar : '/images/profile-placeholder.svg') %>" 
                   alt="Profile" 
                   onerror="this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22150%22 height=%22150%22 viewBox=%220 0 150 150%22%3E%3Crect width=%22150%22 height=%22150%22 fill=%22%23f0f0f0%22/%3E%3Ccircle cx=%2275%22 cy=%2260%22 r=%2225%22 fill=%22%23ddd%22/%3E%3Cpath d=%22M30 130 Q30 100 50 100 L100 100 Q120 100 120 130 Z%22 fill=%22%23ddd%22/%3E%3C/svg%3E'" />
            </a>
            <a href="/auth/logout" title="Logout">🔓</a>
          <% } else { %>
            <a href="/auth/login" title="Login">🔓</a>
          <% } %>
        </nav>
      </div>
    </header>

    <main class="container">
      <div class="detail-wrap">
        <div class="detail-card">
          <div class="detail-inner">
            <div class="detail-media">
              <% /* show large image / thumbnail or placeholder */ %>
              <div class="detail-image" style="background-image: url('<%= book.thumbnail %>')"></div>
            </div>
            <div class="detail-info">
              <h2 class="detail-title"><%= book.title %></h2>

              <!-- ข้อมูลแท็กส์ -->
              <div style="margin-top:10px">
                <strong>🏷️ แท็ก:</strong> 
                <span class="badge" style="background: #f3e5f5; color: #7b1fa2;"><%= book.tags %></span>
              </div>

              <!-- สภาพของสินค้า -->
              <div style="margin-top:10px">
                <strong>⭐ สภาพสินค้า:</strong>
                <% if (book.condition === 'new') { %>
                  <span class="pill condition" style="padding: 4px 12px; border-radius: 20px; font-size: 13px; font-weight: 600; background: #e8f5e8; color: #2e7d32;"><%= book.conditionText %></span>
                <% } else if (book.condition === 'good') { %>
                  <span class="pill condition" style="padding: 4px 12px; border-radius: 20px; font-size: 13px; font-weight: 600; background: #fff3e0; color: #f57c00;"><%= book.conditionText %></span>
                <% } else { %>
                  <span class="pill condition" style="padding: 4px 12px; border-radius: 20px; font-size: 13px; font-weight: 600; background: #fce4ec; color: #c2185b;"><%= book.conditionText %></span>
                <% } %>
              </div>

              <!-- ข้อมูลเจ้าของ -->
              <div style="margin-top:16px; padding-top:12px; border-top:1px solid #e5e5e5;">
                <h3 style="color: var(--muted); font-size: 14px; margin-bottom: 8px;">👤 ข้อมูลเจ้าของสินค้า</h3>
                <div style="background: #f8f9fa; padding: 10px; border-radius: 6px; display:flex; gap:12px; align-items:center; justify-content:space-between;">
                  <div style="display:flex; gap:12px; align-items:center; min-width:0;">
                    <div style="width:64px; height:64px; flex-shrink:0; border-radius:8px; overflow:hidden; background:#fff; display:flex; align-items:center; justify-content:center;">
                      <img src="<%= book.ownerAvatar %>" alt="Owner" style="width:64px; height:64px; object-fit:cover; border-radius:8px;" onerror="this.src='/images/profile-placeholder.svg'" />
                    </div>
                    <div style="min-width:0;">
                      <div style="font-weight:700; font-size:16px; margin-bottom:4px; color:var(--muted);"><%= book.ownerName %></div>
                      <% if (book.owner_username) { %>
                        <div style="color:#888; font-size:13px; margin-bottom:4px;">@<%= book.owner_username %></div>
                      <% } %>
                      <div style="color:#777; font-size:13px;">📍 <%= book.ownerLocation %></div>
                    </div>
                  </div>
                  <div style="flex-shrink:0; display:flex; flex-direction:column; gap:8px; align-items:flex-end;">
                    <% if (book.ownerId && book.ownerActive) { %>
                      <a href="/users/<%= book.ownerId %>" class="btn" style="text-decoration:none; padding:8px 12px; border-radius:8px; background:#ffb27a; color:white; font-weight:700;">ดูหน้าโปรไฟล์</a>
                      <a href="/messages?open=<%= book.ownerId %>" class="btn" style="text-decoration:none; padding:8px 12px; border-radius:8px; background:#fff; color:#444; border:1px solid rgba(0,0,0,0.08); display:flex; align-items:center; gap:8px;">
                        <span style="display:inline-block; width:18px; height:18px; text-align:center;">💬</span>
                        <span style="font-weight:600;">แชทกับเจ้าของ</span>
                      </a>
                    <% } else { %>
                      <span style="font-size:13px; color:#999;">โปรไฟล์ไม่พร้อมใช้งาน</span>
                    <% } %>
                  </div>
                </div>
              </div>

              <!-- ข้อมูลโพสต์ -->
              <div style="margin-top:12px; padding-top:12px; border-top:1px solid #e5e5e5;">
                <h3 style="color: var(--muted); font-size: 14px; margin-bottom: 8px;">📅 ข้อมูลการโพสต์</h3>
                <div style="font-size: 13px; color: #666;">
                  <div style="margin-bottom: 4px;">
                    <strong>รหัสสินค้า:</strong> #<%= book.id %>
                  </div>
                  <div style="margin-bottom: 4px;">
                    <strong>โพสต์เมื่อ:</strong> <%= book.createdDate %>
                  </div>
                  <% if (book.updatedDate) { %>
                    <div>
                      <strong>อัปเดตล่าสุด:</strong> <%= book.updatedDate %>
                    </div>
                  <% } %>
                </div>
              </div>

              <!-- คำอธิบายรายละเอียด -->
              <div style="margin-top:20px; padding-top:16px; border-top:2px solid #f0f0f0;">
                <h3 style="color: var(--muted); font-size: 16px; margin-bottom: 8px;">📝 รายละเอียดเพิ่มเติม</h3>
                <div class="description-box" style="background: #f8f9fa; padding: 12px; border-radius: 8px; line-height: 1.5; white-space: pre-wrap;">
                  <%= book.description %>
                </div>
              </div>

              <% if (user && user.id && book.ownerId && user.id === book.ownerId) { %>
                <div style="margin-top:20px; padding:14px; border-radius:10px; background:#fffaf3; border:1px solid rgba(255,165,0,0.25); display:flex; gap:10px; flex-wrap:wrap;">
                  <strong style="color:#e67e22;">เครื่องมือเจ้าของ:</strong>
                  <a href="/books/<%= book.id %>/edit" class="btn btn-primary" style="text-decoration:none;">✏️ แก้ไขรายการนี้</a>
                  <a href="/books" class="btn btn-ghost" style="text-decoration:none;">📚 กลับไปดูรายการของฉัน</a>
                </div>
              <% } %>

              <!-- ข้อมูลการแลกเปลี่ยน -->
              <div style="margin-top:20px; padding-top:16px; border-top:2px solid #f0f0f0;">
                <h3 style="color: var(--muted); font-size: 16px; margin-bottom: 12px;">🔄 ข้อมูลการแลกเปลี่ยน</h3>
                
                <div style="background: #fafafa; padding: 12px; border-radius: 8px; margin-bottom: 12px;">
                  <div style="margin-bottom: 8px;">
                    <strong>💫 ต้องการแลกกับ:</strong> 
                    <span style="color: var(--accent-700); font-weight: 600;"><%= book.wanted %></span>
                  </div>
                  <div style="margin-bottom: 8px;">
                    <strong>📍 สถานที่แลก:</strong> 
                    <span style="color: var(--accent-700);"><%= book.exchangeLocation %></span>
                  </div>
                  <div>
                    <strong>✅ สถานะ:</strong>
                    <span class="pill <%= book.isAvailable ? 'available' : 'unavailable' %>">
                      <%= book.availabilityText %>
                    </span>
                  </div>
                </div>

                <!-- ปุ่มไปหน้าแบบฟอร์มขอแลกเปลี่ยน -->
                <% if (book.isAvailable) { %>
                  <div style="display:flex; gap:8px; margin-top:16px;">
                    <a href="/exchange/request/<%= book.id %>" class="btn btn-primary" style="flex:1; text-align:center; text-decoration:none;">ไปหน้าแบบฟอร์มขอแลกเปลี่ยน</a>
                    <button type="button" class="btn btn-ghost" onclick="window.history.back()">← กลับ</button>
                  </div>
                <% } else { %>
                  <div class="alert" style="padding:12px; background:#ffe6e6; border:1px solid #ffcccc; border-radius:8px; color:#cc0000; margin-top: 12px;">
                    ⚠️ สินค้านี้ไม่พร้อมสำหรับการแลกเปลี่ยนในขณะนี้
                  </div>
                  <button class="btn btn-ghost" style="margin-top:12px; width: 100%;" onclick="window.history.back()">← กลับไปดูสินค้าอื่น</button>
                <% } %>
              </div>
            </div>
          </div>

          <!-- chat removed per request -->
        </div>
      </div>
    </main>

    <%- include('../partials/footer_unified') %>
  <%- include('../partials/report_footer') %>
  </body>
</html>
