<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Books</title>
  </head>
  <body>
    <h1>All Books</h1>
    <a href="/">Home</a> | <a href="/books/new">Add book</a>
    <ul>
      <% if (books && books.length) { %>
        <% books.forEach(b => { %>
          <li><a href="/books/<%= b.id %>"><%= b.title %></a> — <%= b.author %></li>
        <% }) %>
      <% } else { %>
        <li>No books</li>
      <% } %>
    </ul>
  </body>
  </html>
