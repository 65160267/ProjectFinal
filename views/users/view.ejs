<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title><%= user.full_name %> - โปรไฟล์</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/performance.css" />
    <style>
      /* Enhanced public profile styles */
      body {
        background: #f8f9fa;
      }
      
      .profile-hero {
        background: linear-gradient(135deg, #ffddab 0%, #ffc284 100%);
        padding: 30px 0 80px 0;
        position: relative;
        overflow: hidden;
        min-height: 120px;
      }
      .profile-hero::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -10%;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%);
        border-radius: 50%;
        animation: float 6s ease-in-out infinite;
      }
      .profile-hero::after {
        content: '';
        position: absolute;
        bottom: -30%;
        left: -5%;
        width: 400px;
        height: 400px;
        background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
        border-radius: 50%;
        animation: float 8s ease-in-out infinite;
      }
      @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
      }
      .profile-card-modern {
        background: white;
        border-radius: 24px;
        padding: 40px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.1);
        position: relative;
        z-index: 10;
        max-width: 1000px;
        margin: -60px auto 0;
      }
      .profile-header {
        display: flex;
        gap: 24px;
        align-items: flex-start;
        flex-wrap: wrap;
      }
      .profile-avatar-modern {
        position: relative;
        flex-shrink: 0;
      }
      .profile-avatar-modern img {
        width: 140px;
        height: 140px;
        object-fit: cover;
        border-radius: 20px;
        border: 5px solid white;
        box-shadow: 0 10px 40px rgba(0,0,0,0.15);
        background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
      }
      .profile-avatar-modern::after {
        content: '✓';
        position: absolute;
        bottom: 5px;
        right: 5px;
        background: #10b981;
        color: white;
        width: 32px;
        height: 32px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 16px;
        border: 3px solid white;
        box-shadow: 0 2px 8px rgba(16,185,129,0.4);
      }
      .profile-info-modern {
        flex: 1;
        min-width: 300px;
      }
      .profile-name-modern {
        font-size: 32px;
        font-weight: 800;
        color: #1a202c;
        margin: 0 0 8px 0;
        letter-spacing: -0.5px;
      }
      .profile-username-modern {
        color: #64748b;
        font-size: 16px;
        margin-bottom: 16px;
        font-weight: 500;
      }
      .profile-badges {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        margin-bottom: 20px;
      }
      .badge-modern {
        padding: 8px 16px;
        border-radius: 25px;
        font-size: 13px;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 6px;
      }
      .badge-location {
        background: #fef3c7;
        color: #92400e;
      }
      .badge-verified {
        background: #fff5e6;
        color: #c05621;
      }
      .profile-stats {
        display: flex;
        gap: 24px;
        padding: 20px 0;
        border-top: 1px solid #e5e7eb;
        margin-top: 16px;
      }
      .stat-item {
        text-align: center;
      }
      .stat-value {
        font-size: 24px;
        font-weight: 800;
        color: #e59c60;
        display: block;
        margin-bottom: 4px;
      }
      .stat-label {
        font-size: 13px;
        color: #6b7280;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }
      .profile-actions {
        display: flex;
        gap: 12px;
        margin-top: 20px;
        flex-wrap: wrap;
      }
      .btn-primary-modern {
        padding: 12px 24px;
        border-radius: 12px;
        background: linear-gradient(135deg, #e59c60, #d88c51);
        color: white;
        text-decoration: none;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
        border: none;
        box-shadow: 0 4px 12px rgba(229, 156, 96, 0.3);
      }
      .btn-primary-modern:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(229, 156, 96, 0.4);
      }
      .btn-secondary-modern {
        padding: 12px 24px;
        border-radius: 12px;
        background: white;
        color: #e59c60;
        text-decoration: none;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        border: 2px solid #e59c60;
        transition: all 0.3s ease;
      }
      .btn-secondary-modern:hover {
        background: #e59c60;
        color: white;
        transform: translateY(-2px);
      }
      .books-section {
        max-width: 1200px;
        margin: 50px auto;
        padding: 0 20px;
      }
      .section-title {
        font-size: 28px;
        font-weight: 800;
        color: #1a202c;
        margin-bottom: 28px;
        display: flex;
        align-items: center;
        gap: 12px;
        padding-bottom: 16px;
        border-bottom: 3px solid #ffc284;
      }
      .books-grid-modern {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 24px;
        align-items: stretch;
      }
      .book-card-modern {
        background: white;
        border-radius: 16px;
        overflow: hidden;
        box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        text-decoration: none;
        color: inherit;
        display: flex;
        flex-direction: column;
        border: 1px solid #f0f0f0;
        height: 100%;
      }
      .book-card-modern:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 50px rgba(229, 156, 96, 0.2);
        border-color: #e59c60;
      }
      .book-card-image {
        width: 100%;
        height: 240px;
        object-fit: cover;
        background: linear-gradient(135deg, #f5f7fa, #c3cfe2);
      }
      .book-card-body {
        padding: 20px;
        display: flex;
        flex-direction: column;
        flex: 1;
        position: relative;
      }
      .book-card-title {
        font-size: 18px;
        font-weight: 700;
        color: #1a202c;
        margin-bottom: 8px;
        line-height: 1.4;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        min-height: 50px;
      }
      .book-card-author {
        color: #64748b;
        font-size: 14px;
        margin-bottom: 12px;
        min-height: 20px;
      }
      .book-card-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding-top: 12px;
        border-top: 1px solid #f1f5f9;
        margin-top: auto;
        margin-bottom: 12px;
      }
      /* Actions area under the body to hold centered button */
      .book-card-actions {
        display: flex;
        justify-content: center;
        width: 100%;
        padding: 0 20px 20px 20px;
        box-sizing: border-box;
      }
      .book-card-id {
        font-size: 12px;
        color: #94a3b8;
      }
      .book-card-arrow {
        color: #667eea;
        font-size: 20px;
      }
      .btn-view-detail {
        display: inline-flex !important;
        justify-content: center;
        align-items: center;
        gap: 8px;
        padding: 12px 32px !important;
        background: linear-gradient(135deg, #ff9a56, #ff6b6b) !important;
        color: #fff !important;
        text-align: center !important;
        text-decoration: none !important;
        border-radius: 10px !important;
        font-weight: 600 !important;
        font-size: 14px !important;
        transition: all 0.3s ease !important;
        box-shadow: 0 4px 12px rgba(255,154,86,0.3) !important;
        white-space: nowrap !important;
      }
      .btn-view-detail:hover {
        background: linear-gradient(135deg, #ff8a3d, #ff5252) !important;
        transform: translateY(-2px) !important;
        box-shadow: 0 6px 20px rgba(255,154,86,0.4) !important;
      }
      .empty-state-modern {
        text-align: center;
        padding: 80px 40px;
        background: white;
        border-radius: 20px;
        border: 2px dashed #e5e7eb;
        margin: 20px 0;
      }
      .empty-state-icon {
        font-size: 80px;
        margin-bottom: 20px;
        opacity: 0.4;
        filter: grayscale(50%);
      }
      .empty-state-title {
        font-size: 22px;
        font-weight: 700;
        color: #475569;
        margin-bottom: 12px;
      }
      .empty-state-text {
        font-size: 15px;
        color: #64748b;
        line-height: 1.6;
      }
      @media (max-width: 768px) {
        .profile-hero {
          padding: 25px 0 70px 0;
          min-height: 100px;
        }
        .profile-card-modern {
          padding: 24px;
          margin: -50px 16px 0;
        }
        .profile-header {
          flex-direction: column;
          align-items: center;
          text-align: center;
        }
        .profile-name-modern {
          font-size: 24px;
        }
        .profile-stats {
          justify-content: center;
          flex-wrap: wrap;
        }
        .profile-actions {
          justify-content: center;
          width: 100%;
        }
        .btn-primary-modern,
        .btn-secondary-modern {
          width: 100%;
          justify-content: center;
        }
        .books-grid-modern {
          grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
          gap: 16px;
          align-items: stretch;
        }
        .book-card-image {
          height: 200px;
        }
        .section-title {
          font-size: 22px;
        }
      }
    </style>
  </head>
  <body>
    <header class="site-header">
      <div class="header-inner container">
        <div class="brand">
          <a href="/dashboard" class="brand">SwapABook</a>
        </div>
        <nav class="header-actions" style="display:flex;align-items:center;gap:14px;">
          <a href="/books" title="รายการหนังสือ" style="text-decoration:none;">📚</a>
          <a href="/messages" title="ข้อความ" style="text-decoration:none;">💬</a>
          <a href="/dashboard" title="กลับหน้าแรก" style="text-decoration:none;">🏠</a>
          <% if (sessionUser && sessionUser.username) { %>
            <a href="/user" class="header-user" title="โปรไฟล์ของฉัน" style="display:inline-flex;align-items:center;gap:6px;text-decoration:none;padding:4px 10px;border:1px solid rgba(255,255,255,0.4);border-radius:30px;background:rgba(255,255,255,0.18);backdrop-filter:blur(4px);">
              <img class="user-avatar" 
                   src="<%= (sessionUser && sessionUser.avatar && sessionUser.avatar.startsWith('/')) ? sessionUser.avatar : (sessionUser && sessionUser.avatar ? '/uploads/' + sessionUser.avatar : '/images/profile-placeholder.svg') %>" 
                   alt="avatar" 
                   style="width:32px;height:32px;border-radius:50%;object-fit:cover;border:2px solid #fff;box-shadow:0 0 0 2px rgba(255,255,255,0.3);background:#fff;" 
                   onerror="this.src='/images/profile-placeholder.svg'" />
              <% if (sessionUser && sessionUser.username && isNaN(Number(sessionUser.username))) { %>
                <span class="username" style="font-size:13px;font-weight:600;color:#fff;max-width:80px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;"><%= sessionUser.username %></span>
              <% } %>
            </a>
            <a href="/auth/logout" title="ออกจากระบบ" style="text-decoration:none;">🔓</a>
          <% } else { %>
            <a href="/auth/login" title="เข้าสู่ระบบ" style="text-decoration:none;">🔓</a>
          <% } %>
        </nav>
      </div>
    </header>

    <!-- Hero Section with Gradient Background -->
    <div class="profile-hero"></div>

    <!-- Profile Card Section -->
    <div class="container" style="margin-top: 0;">
      <div class="profile-card-modern">
        <div class="profile-header">
          <!-- Avatar with Verified Badge -->
          <div class="profile-avatar-modern">
            <img src="<%= user.avatar %>" alt="<%= user.full_name %>" onerror="this.src='/images/profile-placeholder.svg'" />
          </div>

          <!-- Profile Information -->
          <div class="profile-info-modern">
            <h1 class="profile-name-modern"><%= user.full_name %></h1>
            <div class="profile-username-modern">@<%= user.username %></div>

            <!-- Badges -->
            <div class="profile-badges">
              <span class="badge-modern badge-location">
                📍 <%= user.location || 'ไม่ระบุที่อยู่' %>
              </span>
              <span class="badge-modern badge-verified">
                ✓ ผู้ใช้ยืนยันแล้ว
              </span>
            </div>

            <!-- Statistics -->
            <div class="profile-stats">
              <div class="stat-item">
                <span class="stat-value"><%= Array.isArray(userBooks) ? userBooks.length : 0 %></span>
                <span class="stat-label">รายการ</span>
              </div>
              <div class="stat-item">
                <span class="stat-value"><%= (typeof userStats !== 'undefined' && userStats && userStats.exchanges) ? userStats.exchanges : 0 %></span>
                <span class="stat-label">แลกสำเร็จ</span>
              </div>
              <div class="stat-item">
                <span class="stat-value"><%= (typeof userStats !== 'undefined' && userStats && userStats.views) ? userStats.views : 0 %></span>
                <span class="stat-label">เข้าชม</span>
              </div>
            </div>

            <!-- Action Buttons -->
            <div class="profile-actions">
              <a href="/messages?open=<%= user.id %>" class="btn-primary-modern">
                💬 ส่งข้อความ
              </a>
              <a href="/dashboard" class="btn-secondary-modern">
                🏠 กลับหน้าแรก
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Books Section -->
    <div class="books-section">
      <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 28px;">
        <h2 class="section-title" style="margin: 0; border: none; padding: 0;">
          📚 รายการหนังสือของ <%= user.full_name %>
        </h2>
        <% if (Array.isArray(userBooks) && userBooks.length > 0) { %>
          <div style="background: linear-gradient(135deg, #e59c60, #d88c51); color: white; padding: 8px 20px; border-radius: 25px; font-weight: 700; font-size: 14px; box-shadow: 0 4px 12px rgba(229, 156, 96, 0.3);">
            <%= userBooks.length %> รายการ
          </div>
        <% } %>
      </div>

      <% if (Array.isArray(userBooks) && userBooks.length > 0) { %>
        <div class="product-grid" style="max-width: 1200px; margin: 0 auto;">
          <% userBooks.forEach(function(b, index) { %>
            <a href="/books/<%= b.id %>" class="product-card" style="text-decoration: none; color: inherit; position: relative; overflow: hidden; border: 1px solid #f0f0f0;">
              <% if (index < 3) { %>
                <div style="position: absolute; top: 8px; right: 8px; background: #ff4757; color: white; padding: 4px 8px; border-radius: 12px; font-size: 11px; font-weight: 700; z-index: 2;">
                  🆕 ใหม่
                </div>
              <% } %>
              
              <div class="product-thumb" data-thumb="<%= b.thumbnail || '/images/placeholder.png' %>" style="position: relative; height: 240px;">
                <div style="position: absolute; bottom: 0; left: 0; right: 0; background: linear-gradient(transparent, rgba(0,0,0,0.7)); height: 40px;"></div>
              </div>
              
              <div class="product-body" style="padding: 14px;">
                <div class="product-title" style="font-weight: 700; color: var(--muted); margin-bottom: 8px; font-size: 16px;">
                  📖 <%= b.title || 'หนังสือไม่ระบุชื่อ' %>
                </div>
                
                <div style="display: flex; gap: 6px; align-items: center; margin-bottom: 8px; flex-wrap: wrap;">
                  <span style="font-size: 12px; color: var(--muted);">🔄 แลกกับ:</span>
                  <span class="badge" style="font-size: 11px;"><%= b.tags || 'ไม่ระบุ' %></span>
                  <% if (b.condition) { %>
                    <span class="pill condition" style="font-size: 11px;">
                      <%= (b.condition === 'new' ? '✨ ใหม่' : (b.condition === 'good' ? '👍 ดี' : '📝 ผ่านการใช้งาน')) %>
                    </span>
                  <% } %>
                </div>
                
                <div class="product-meta" style="margin-bottom: 12px; font-size: 13px; color: #666;">
                  📍 <%= b.location || 'ไม่ทราบที่อยู่' %>
                </div>
              </div>
            </a>
          <% }); %>
        </div>
      <% } else { %>
        <div class="empty-state-modern">
          <div class="empty-state-icon">📚</div>
          <div class="empty-state-title">ยังไม่มีหนังสือในคลัง</div>
          <div class="empty-state-text">
            <%= user.full_name %> ยังไม่ได้เพิ่มหนังสือเข้าสู่ระบบ<br>
            กลับมาตรวจสอบอีกครั้งในภายหลัง
          </div>
        </div>
      <% } %>
    </div>

    <div style="height: 40px;"></div>

    <script>
      document.querySelectorAll('.product-thumb[data-thumb]').forEach(el => {
        const url = el.getAttribute('data-thumb') || '/images/placeholder.png';
        el.style.backgroundImage = `url(${url})`;
      });
    </script>
    <%- include('../partials/footer_unified') %>
    <%- include('../partials/report_footer') %>
  </body>
</html>