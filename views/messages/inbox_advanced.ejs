<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>แชท (ขั้นสูง)</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div style="display:flex;gap:12px;padding:12px">
      <aside style="width:300px">
        <h3>กล่องข้อความ (ขั้นสูง)</h3>
        <div id="conversations">
          <% if (convos && convos.length) { %>
            <% convos.forEach(function(c){ %>
              <div class="convo-item" data-room="<%= c.room %>">
                <a href="/messages-advanced?open=<%= c.room %>"><%= c.room %></a>
              </div>
            <% }) %>
          <% } else { %>
            <div style="opacity:0.7">ยังไม่มีการสนทนา</div>
          <% } %>
        </div>
      </aside>
      <main style="flex:1">
        <div id="chatArea">
          <div id="chatHeader">เลือกการสนทนา</div>
          <div id="chatWindow" style="min-height:320px;border:1px solid #ddd;padding:12px;margin-top:8px;background:#fff"></div>
          <div id="statusLine" style="font-size:12px;color:#666;margin-top:6px"></div>
          <div id="composer" style="margin-top:8px;display:flex;gap:8px;align-items:center">
            <input id="msgInput" type="text" style="flex:1;padding:8px" placeholder="พิมพ์ข้อความ..." />
            <button id="sendBtn">ส่ง</button>
            <button id="reactBtn" title="ส่งปฏิกิริยา">😊</button>
          </div>
        </div>
      </main>
    </div>
    <script>
      window.__ME__ = <%- JSON.stringify(me || null) %>;
    </script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/chat-advanced.js"></script>
    <%- include('../partials/footer_unified') %>
    <%- include('../partials/report_footer') %>
  </body>
</html>
