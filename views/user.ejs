<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>User Profile</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header class="site-header">
      <div class="header-inner container">
        <div class="brand">Trade Web</div>
        <nav class="header-actions">
          <a href="/marketplace">Marketplace</a>
          <a href="/books/new">Add Product</a>
          <a href="/books">My List</a>
          <% if (user && user.username) { %>
            <a href="/user" class="header-user" title="Your profile">
              <img class="user-avatar" src="<%= user.avatar || '/images/profile-placeholder.png' %>" alt="Profile" />
              <span class="username"><%= user.username %></span>
            </a>
            <a href="/auth/logout">Logout</a>
          <% } else { %>
            <a href="/auth/login">Login</a>
          <% } %>
        </nav>
      </div>
    </header>

    <main class="container">
      <div class="card" style="max-width:700px;margin:24px auto;display:flex;gap:18px;align-items:center">
        <div style="flex:0 0 140px;display:flex;flex-direction:column;align-items:center;gap:10px">
          <div style="width:120px;height:120px;border-radius:999px;border:3px solid var(--accent-700);overflow:hidden;display:flex;align-items:center;justify-content:center;background:#fff">
            <img src="<%= user.avatar || '/images/profile-placeholder.png' %>" alt="Profile Picture" style="width:100%;height:100%;object-fit:cover" />
          </div>
          <input type="file" />
        </div>
        <div style="flex:1">
          <h2><%= user.full_name || user.username %></h2>
          <div class="muted">อายุ: --</div>
          <div class="muted">ที่อยู่: <%= user.location || '--' %></div>
          <div style="margin-top:12px">ประวัติการแลกเปลี่ยน</div>
          <ul class="muted">
            <li>ยังไม่มีประวัติ</li>
          </ul>
          <div style="margin-top:12px">
            <button class="btn">ตั้งค่า</button>
            <a class="btn btn-danger" href="/auth/logout" style="margin-left:8px">Logout</a>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>