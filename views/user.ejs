<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>User Profile</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header class="site-header">
      <div class="header-inner container">
        <div class="brand">SwapABook</div>
        <form class="search" action="/" method="get">
          <input name="q" placeholder="🔍 ค้นหาหนังสือ..." />
          <button type="submit">🔍</button>
        </form>
        <nav class="header-actions">
          <a href="/dashboard" title="หน้าหลัก" style="padding: 8px 12px; border-radius: 8px; transition: all 0.2s;">🏠</a>
          <a href="/books" title="รายการหนังสือ" style="padding: 8px 12px; border-radius: 8px; transition: all 0.2s;">📚</a>
          <a href="/books/new" title="เพิ่มหนังสือใหม่" style="padding: 8px 12px; border-radius: 8px; transition: all 0.2s;">➕</a>
          <% if (user && user.username) { %>
            <a href="/user" class="header-user" title="โปรไฟล์ของคุณ" style="display: flex; align-items: center; gap: 8px; padding: 6px 12px; background: rgba(255,255,255,0.15); border-radius: 20px; text-decoration: none; transition: all 0.3s; backdrop-filter: blur(10px); border: 2px solid rgba(255,255,255,0.3);">
              <img class="user-avatar" 
                   src="<%= user.avatar || '/images/profile-placeholder.png' %>" 
                   alt="Profile" 
                   style="width: 36px; height: 36px; border-radius: 50%; object-fit: cover; transition: all 0.3s;" />
              <div style="display: flex; flex-direction: column; align-items: flex-start;">
                <span class="username" style="color: white; font-weight: 700; font-size: 14px; line-height: 1.2;"><%= user.full_name || user.username %></span>
                <span style="color: rgba(255,255,255,0.8); font-size: 11px; line-height: 1;">โปรไฟล์</span>
              </div>
              <span style="color: rgba(255,255,255,0.8); font-size: 12px;">👤</span>
            </a>
            <a href="/auth/logout" title="ออกจากระบบ" style="padding: 8px 12px; border-radius: 8px; transition: all 0.2s; margin-left: 8px;">🔓</a>
          <% } else { %>
            <a href="/auth/login" title="เข้าสู่ระบบ" style="padding: 8px 12px; border-radius: 8px; transition: all 0.2s;">🔓</a>
          <% } %>
        </nav>
      </div>
    </header>

    <main class="container">
      <div class="profile-wrap">
        <div class="profile-card card">
          <div class="profile-main">
            <div class="profile-media">
              <div class="avatar">
                <img src="<%= user.avatar || '/images/profile-placeholder.png' %>" alt="Avatar" />
              </div>
              <!-- avatar upload moved to /user/edit -->
            </div>
            <div class="profile-info">
              <h2 class="profile-name"><%= user.full_name || user.username %></h2>
              <div class="muted">username: <%= user.username %></div>
              <div class="muted">ที่อยู่: <%= user.location || '--' %></div>
              <div class="profile-stats">
                <div><strong>0</strong><div class="muted">แลกเรียบร้อย</div></div>
                <div><strong>0</strong><div class="muted">รายการของคุณ</div></div>
              </div>
              <div style="margin-top:12px">
                <a class="btn btn-ghost" href="/user/edit">แก้ไขโปรไฟล์</a>
                <a class="btn btn-danger" href="/auth/logout" style="margin-left:8px">Logout</a>
              </div>
            </div>
          </div>

          <hr />
          <div class="profile-history">
            <h3>ประวัติการแลกเปลี่ยน</h3>
            <p class="muted">ยังไม่มีประวัติการแลกเปลี่ยน</p>
          </div>
        </div>

        <aside class="profile-side card">
          <h4>รายการของฉัน</h4>
          <p class="muted">ยังไม่มีรายการ</p>
        </aside>
      </div>
    </main>

    <footer class="site-footer">
      <div class="container footer-bottom">
        <div class="copyright">Swap A Book <%= new Date().getFullYear() %></div>
      </div>
    </footer>
  </body>
</html>